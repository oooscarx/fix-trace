# Desktop GUI 使用指南

完整、持续维护的指南见 [FixTrace Desktop](desktop.md)。真实 Tauri 开发窗口：

```bash
cd apps/fixtrace-desktop
npm ci
npm run tauri -- dev
```

课堂入口为 `./demo/presentation.sh desktop`；`mock-gui` 仅用于明确标识的固定视觉流程。打包与签名限制见 [U9 发布验证](u9-release-verification.md)。
